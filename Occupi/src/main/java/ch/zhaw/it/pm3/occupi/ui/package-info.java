/**
 * User interface (UI) package for the Occupi application.
 *
 * <p>This package contains classes and components responsible for presenting
 * application data to users and processing user interactions. Typical
 * responsibilities include view rendering, user input handling and
 * validation, composing UI-specific data transfer objects (DTOs), and
 * coordinating with controller and model layers.</p>
 *
 */
package ch.zhaw.it.pm3.occupi.ui;